# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  action_mailer:
    smtp_settings:
      address: localhost
      port: 1025
    default_url_options:
      host: localhost
      port: 3000
  application:
    default_url_options:
      host: 'localhost:3000'
    routes:
      default_url_options:
        host: 'localhost:3000'
  secret_key_base: e340983f509823823f087e92349783b24789478af65498349873a49874b39875947954b71231a23c24153515355234d3ff56987b46978934597b4639875987b9
test:
  application:
    default_url_options:
      host: www.example.com
    routes:
      default_url_options:
        host: www.example.com
  secret_key_base: a9f375b1476b4577d8ab0f5fc34a3fdf467d668e8f1ed0d1d14f7416c031131f8e1a5bc17187f069faa9ed8089568e9c10b3582d6599ad11997f1ea5f948d036

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  action_mailer:
    smtp_settings:
      address: localhost
      port: 1025
    default_url_options:
      host: localhost
      port: 3000
  application:
    default_url_options:
      host: 't-manager-board.herokuapp.com'
    routes:
      default_url_options:
        host: 't-manager-board.herokuapp.com'
  # secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  secret_key_base: e340983f509823823f087e92349783b24789478af65498349873a49874b39875947954b71231a23c24153515355234d3ff56987b46978934597b4639875987b9
